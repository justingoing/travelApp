getLegsAsSVG:
cognitive complexity could be reduced into separate functions. e.g. wrap around edge method.

SVG could be its own class.

Is there a faster way other than appending?

convert to decimal:
instead of boolean flags, call new method inside the conditional.
e.g. foundSecond foundMinute
also in the conditional, call found methods. foundMinute, foundSecond