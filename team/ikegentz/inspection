Need separate SVG class

Move to new SVG file 
	GetSVGFromFile
	getLegsAsSVG
	addSvgLeg
	addSvgCircle
	getMappedCoords
	normalizeLat
	normaliizeLong

Make separate Parse class

Move to new Parse file
	validateLatLong
	convertToDecimal

getLegsAsSVG
	'svg' string at top should be a static constant for the class
	Change structure of 'if' so that instead of all the code being inside if, we just do an early return
	We are calling 'new' a lot (4 times for every destination), we should move these outside the loop and just reset coords
	
